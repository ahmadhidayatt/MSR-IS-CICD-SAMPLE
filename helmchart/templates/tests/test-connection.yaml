apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webmethods11-app.fullname" . }}-test-connection"
  labels:
    {{- include "webmethods11-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webmethods11-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
